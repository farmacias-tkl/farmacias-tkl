"use client"; import { useState } from "react"; import { useForm } from "react-hook-form"; import { z } from "zod"; import { zodResolver } from "@hookform/resolvers/zod"; const schema = z.object({ currentPassword: z.string().min(1, "Obligatoria"), newPassword: z.string().min(8, "Minimo 8 caracteres").regex(/[A-Z]/, "Mayuscula requerida").regex(/[0-9]/, "Numero requerido"), confirmPassword: z.string() }).refine(d => d.newPassword === d.confirmPassword, { message: "No coinciden", path: ["confirmPassword"] }); type F = z.infer<typeof schema>; export default function Page() { const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const { register, handleSubmit, formState: { errors } } = useForm<F>({ resolver: zodResolver(schema) }); async function onSubmit(data: F) { setLoading(true); setError(""); const res = await fetch("/api/me/change-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ currentPassword: data.currentPassword, newPassword: data.newPassword }) }); const json = await res.json(); if (!res.ok) { setError(json.error || "Error"); setLoading(false); return; } window.location.href = "/dashboard"; } return (<div style={{minHeight:"100vh",display:"flex",alignItems:"center",justifyContent:"center",background:"#f4f5f7"}}><div style={{background:"white",padding:"2rem",borderRadius:"1rem",width:"100%",maxWidth:"420px",border:"1px solid #e3e6ea"}}><h1 style={{fontSize:"1.2rem",fontWeight:600,marginBottom:"0.5rem"}}>Cambia tu contrasena</h1><p style={{fontSize:"0.85rem",color:"#6b7280",marginBottom:"1.5rem"}}>Obligatorio en el primer ingreso.</p><form onSubmit={handleSubmit(onSubmit)} style={{display:"flex",flexDirection:"column",gap:"1rem"}}><div><label style={{fontSize:"0.8rem",fontWeight:500,display:"block",marginBottom:"0.25rem"}}>Contrasena actual</label><input {...register("currentPassword")} type="password" style={{width:"100%",padding:"0.5rem 0.75rem",border:"1px solid #d1d5db",borderRadius:"0.5rem",boxSizing:"border-box"}} /></div><div><label style={{fontSize:"0.8rem",fontWeight:500,display:"block",marginBottom:"0.25rem"}}>Nueva contrasena</label><input {...register("newPassword")} type="password" style={{width:"100%",padding:"0.5rem 0.75rem",border:"1px solid #d1d5db",borderRadius:"0.5rem",boxSizing:"border-box"}} />{errors.newPassword && <p style={{color:"red",fontSize:"0.75rem",marginTop:"0.25rem"}}>{errors.newPassword.message}</p>}</div><div><label style={{fontSize:"0.8rem",fontWeight:500,display:"block",marginBottom:"0.25rem"}}>Confirmar contrasena</label><input {...register("confirmPassword")} type="password" style={{width:"100%",padding:"0.5rem 0.75rem",border:"1px solid #d1d5db",borderRadius:"0.5rem",boxSizing:"border-box"}} />{errors.confirmPassword && <p style={{color:"red",fontSize:"0.75rem",marginTop:"0.25rem"}}>{errors.confirmPassword.message}</p>}</div>{error && <p style={{color:"red",background:"#fef2f2",padding:"0.5rem",borderRadius:"0.5rem",fontSize:"0.85rem"}}>{error}</p>}<button type="submit" disabled={loading} style={{padding:"0.75rem",background:"#1d4ed8",color:"white",border:"none",borderRadius:"0.5rem",fontWeight:600,cursor:loading?"not-allowed":"pointer",opacity:loading?0.7:1}}>{loading?"Guardando...":"Guardar contrasena"}</button></form></div></div>); }

