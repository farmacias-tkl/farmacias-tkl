import { NextRequest, NextResponse } from "next/server";
import { auth } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { can, requireAuth } from "@/lib/permissions";
export async function PATCH(req: NextRequest, { params }: { params: { id: string } }) { const session = await auth(); const authErr = requireAuth(session); if (authErr) return NextResponse.json({ error: authErr.error }, { status: authErr.status }); const absence = await prisma.absenceRecord.findUnique({ where: { id: params.id } }); if (!absence) return NextResponse.json({ error: "No encontrada" }, { status: 404 }); if (session!.user.role === "BRANCH_MANAGER" && absence.branchId !== session!.user.branchId) return NextResponse.json({ error: "Sin permisos" }, { status: 403 }); const body = await req.json(); const { status, notes, hasCertificate } = body; if (status && !can.justifyAbsence(session!.user.role)) return NextResponse.json({ error: "Sin permisos" }, { status: 403 }); const updated = await prisma.absenceRecord.update({ where: { id: params.id }, data: { ...(status && { status }), ...(notes !== undefined && { notes }), ...(hasCertificate !== undefined && { hasCertificate }) } }); return NextResponse.json({ data: updated }); }
export async function GET(req: NextRequest, { params }: { params: { id: string } }) { const session = await auth(); const authErr = requireAuth(session); if (authErr) return NextResponse.json({ error: authErr.error }, { status: authErr.status }); const absence = await prisma.absenceRecord.findUnique({ where: { id: params.id }, include: { employee: { select: { id: true, firstName: true, lastName: true, position: { select: { name: true } } } }, branch: { select: { id: true, name: true } } } }); if (!absence) return NextResponse.json({ error: "No encontrada" }, { status: 404 }); return NextResponse.json({ data: absence }); }
